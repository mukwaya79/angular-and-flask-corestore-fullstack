# angular-and-flask-corestore-fullstack

run pip install flask-mysqldb
after that access your terminal and run python idle and then from app import db followed by db.create_all() in the flask section
